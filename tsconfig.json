{
  // Автоматическая генерирует
  "extends": "./.nuxt/tsconfig.json",

  "compilerOptions": {
    // Корневой путь к содержимому
    "baseUrl": "./",
    "paths": {
      "@/assets/*": ["assets/*"],
      "@/components/*": ["components/*"],
      "@/api/*": ["api/*"],
      "@/types/*": ["types/*"],
    }
  }
}
